version: '3.8'

services:
  postgres:
    image: bitnami/postgresql:14.5.0-debian-11-r25
    ports:
      - 5432:5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U calendar_user"]
      timeout: 5s
      interval: 15s
      start_period: 1s
      retries: 10
    deploy:
      mode: replicated
      replicas: 1
    environment:
      - POSTGRESQL_DATABASE=calendar
      - POSTGRESQL_USERNAME=calendar_user
      - POSTGRESQL_PASSWORD=calendar_pass
    volumes:
      - ../migrations:/docker-entrypoint-initdb.d
    networks:
      - app

  rabbitmq:
    image: rabbitmq:3.9.23-management-alpine
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      timeout: 5s
      interval: 15s
      start_period: 1s
      retries: 10
    deploy:
      mode: replicated
      replicas: 1
    environment:
      - RABBITMQ_DEFAULT_USER=rabbit_user
      - RABBITMQ_DEFAULT_PASS=rabbit_pass
    networks:
      - app

  calendar:
    build:
      context: ..
      dockerfile: build/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    deploy:
      mode: global
      replicas: 1
    healthcheck:
      test: "curl --connect-timeout 1 --silent --show-error --fail http://127.0.0.1/health"
      timeout: 5s
      interval: 25s
      start_period: 1s
      retries: 10
    networks:
      - app

#  scheduler:
#  sender:

  nginx:
    build:
      context: ..
      dockerfile: deployments/nginx/Dockerfile
    depends_on:
      calendar:
        condition: service_healthy
    ports:
      - "15672:15672" # rabbitmq
      - "80:80" # calendar
    deploy:
      mode: replicated
      replicas: 1
    healthcheck:
      test: "curl --connect-timeout 1 --silent --show-error --fail http://127.0.0.1/health"
      timeout: 1s
      interval: 15s
      start_period: 1s
      retries: 10
    volumes:
      - ./nginx/calendar.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/rabbitmq-management.conf:/etc/nginx/conf.d/rabbitmq-management.conf:ro
    networks:
      - app

networks:
  app:
